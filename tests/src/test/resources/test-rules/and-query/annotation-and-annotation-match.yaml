# Cmd: mvn -pl migration-cli quarkus:dev -Dquarkus.args="analyze ../applications/demo-spring-boot-todo-app -r ../tests/resources/test-rules/and-query/q1"
# Scanner: rewrite
# Match: true
- category: mandatory
  customVariables: []
  description: SpringBoot to Quarkus
  effort: 1
  labels:
    - konveyor.io/source=springboot
    - konveyor.io/target=quarkus
  links: []
  message: "SpringBoot to Quarkus."
  ruleID: annotation-and-annotation-match
  when:
    condition: |
      java.annotation is 'org.springframework.stereotype.Controller' AND
      java.annotation is 'org.springframework.web.bind.annotation.GetMapping'
  order: 4
  instructions:
    ai:
      - tasks:
          - "TODO"
    manual:
      - todo: "See openrewrite instructions"
